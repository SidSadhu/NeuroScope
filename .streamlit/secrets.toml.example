# Copy this file to `.streamlit/secrets.toml` (git-ignored) and paste your key.
# Get a free Groq API key at https://console.groq.com
# Without a key, NeuroScope runs in its clearly-labeled offline mock mode.
GROQ_API_KEY = "gsk_your_key_here"
